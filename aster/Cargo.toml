[package]
name = "aster"
version = "0.1.0"
edition = "2024"

[dependencies]
lang = { version = "0.1.0", path = "../lang" }
tokenize = { version = "0.1.0", path = "../tokenize" }
gluezy = { version = "0.1.0", path = "../gluezy" }
